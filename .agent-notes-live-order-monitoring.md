# Live order monitoring

Source: https://recharza-platform.vercel.app/api/readiness
Checked: 2026-08-17T15:19:34.828Z

The production readiness endpoint returned `status: ready` with 8/8 required checks ready and a successful PostgreSQL connection. Gmail SMTP is the active account and order email transport. Razorpay is configured in test mode. Supplier writes remain disabled and the FazerCards catalogue check is optional and not ready. The response does not itself prove that an order has been created or paid; runtime logs and database/order records must be checked next.

## Vercel log snapshot

The authenticated Vercel Logs view covered approximately 14:50–15:20 on Aug 17 and displayed 10 error-level entries, zero warnings, and zero fatal entries. Visible recent requests were successful: `/api/readiness` 200, `/api/auth/logout` 200, `/api/account/addresses` 200, `/api/account/orders` 200, `/api/auth/session` 200, `/api/cart` 200, `/account` 200, `/admin` 200, and `/` 200. The visible message was a PostgreSQL SSL-mode compatibility warning, not a checkoutBatchId or order-processing error. No checkout or payment request appeared in the visible latest rows, so further filtering/search is required.

## Filtered order-related logs

The loaded production log rows contained only successful read flows related to orders: `/api/account/orders` 200, `/orders/lookup` 200, and `/account/orders` 200. No `/api/checkout/*`, payment-session, Razorpay webhook, Prisma error, or checkoutBatchId error appeared in the current loaded interval. This means the dashboard currently provides evidence of healthy order-history reads but not of a new checkout or payment transaction.

## Error scan

A DOM-level scan of all currently loaded Vercel log rows found no non-200 status rows and no explicit `error`, `exception`, `failed`, `Prisma`, `checkoutBatchId`, or `SMTP` messages. The Vercel UI’s aggregate error count is not represented in the currently loaded row set, so the result is limited to the visible time window and loaded entries.
