# Live email diagnosis findings

- Live storefront: https://recharza-platform.vercel.app/ (reachable; current public build shows premium storefront, currency switcher, cart/account/support links, and game routes).
- Live readiness: https://recharza-platform.vercel.app/api/readiness
- Readiness response at 2026-08-16T23:07:53.607Z reported `status: ready`, database connection succeeded via `POSTGRES_PRISMA_URL`, and all 8 required checks ready.
- Critical finding: the deployed email check reported `message: "Resend is explicitly selected as the Recharza transactional email transport."` despite the requested Gmail SMTP setup. This confirms the production environment currently has `EMAIL_DELIVERY_PROVIDER` set to `resend` or unset with Resend configuration active; it is not using Gmail SMTP.
- Neon console URL https://console.neon.tech/ is region-blocked in the sandbox with `Access Blocked in Your Region`, so the browser cannot reach the SQL editor from here.
- Vercel dashboard is reachable at https://vercel.com/stand-still and shows project `recharza-platform` under the Stand Still team; the dashboard has Environment Variables and Deployments sections. No Vercel CLI or MCP server was available in the sandbox.
- Local patched code now supports `EMAIL_DELIVERY_PROVIDER=gmail-smtp`, defaults SMTP authentication/sender to `recharza1@gmail.com`, adds SMTP readiness validation, and extends the protected diagnostic endpoint to report SMTP safely.

## Vercel environment audit

The authenticated Vercel project `recharza-platform` is reachable under the Stand Still team. Its Environment Variables page shows `EMAIL_DELIVERY_PROVIDER`, `GMAIL_SMTP_PASSWORD`, `GMAIL_SMTP_USER`, `GOOGLE_MAIL_FROM`, `GMAIL_SMTP_HOST`, and `GMAIL_SMTP_PORT` configured for Production and Preview. The provider row is marked Sensitive and was updated about 15 minutes ago, while the SMTP credentials were updated about one hour ago. Despite these variables being present, the live readiness endpoint still reports Resend, indicating that the provider value is likely still `resend` or that the last production deployment predates the intended provider update. The next safe action is to open the provider variable’s edit form, set both Production and Preview to `gmail-smtp`, save, then redeploy from the patched main branch.

The `EMAIL_DELIVERY_PROVIDER` variable was edited in Vercel and saved as `gmail-smtp` with the existing Production and Preview scope. Vercel now shows the variable as `Updated just now`. The SMTP password was not revealed or changed.

## Deployment verification

Vercel automatically built commit `22adc4b` from `main`. The newest Production deployment is now `Ready` at https://recharza-platform-igx4m7tlu-stand-still.vercel.app and is associated with the production domain. The next verification should query `/api/readiness` on the production domain and the deployment URL, then use the protected email diagnostic route if its authentication requirements are available.

## Production log inspection

The authenticated Vercel Logs view shows the latest deployment serving `/api/readiness` and both Telegram health endpoints with HTTP 200. Filtering the visible log stream for `SMTP` did not reveal a new SMTP authentication or transport failure. The dashboard reports 21 error-level entries overall, but the visible messages from the latest period are PostgreSQL SSL-mode compatibility warnings, not email errors. No actual email send has occurred in the current deployment, so this does not prove mailbox receipt.

## User-confirmed delivery

The user confirmed that the post-deployment test email arrived at `phangchosongja02@gmail.com`, but Gmail placed it in Spam. This verifies production Gmail SMTP authentication, sender configuration, external delivery, and the transactional path. Inbox placement remains a sender-reputation/content-alignment issue and is separate from the original delivery failure.
